Two bugs broke both work pages. Fix both.

BUG 1 — The `active` flag is being applied to the work pages.

Every item was seeded with active = false, and /photography and /videography are 
filtering on it, so both pages are now empty. That was never the intent.

`active` controls ONE thing only: whether an item appears in the homepage carousel.

- Remove any `active` condition from /photography, /videography, and the /api/photos 
  and /api/videos endpoints. These must return every item of the matching type 
  regardless of `active`.
- The ONLY place `active = true` should be filtered is the homepage carousel query.
- Confirm to me which queries you changed.

BUG 2 — The seeded category values don't match the site's filters.

You reported categories including "Snow" and "Events." Those are not the categories 
this site uses. The only valid values are exactly:

  Music
  Sports
  Hospitality & Events

That mismatch is why the category filters are broken.

- Update every seeded row so its `category` is one of those three exact strings. Map 
  "Snow" and any other snow/ski/action-sports value to "Sports." Map "Events" and any 
  events/hospitality value to "Hospitality & Events." Preserve "Music" as-is.
- Constrain the category field so only those three values can be written.
- Make the portal's category dropdown offer exactly those three options and nothing 
  else.
- If any item's original category is ambiguous, list it for me rather than guessing.

AFTER BOTH FIXES

Verify and report back with actual counts:
- /photography renders all 42 photos, and each of the three filter buttons returns the 
  correct subset
- /videography renders all 12 videos, same check
- The homepage carousel is empty (correct — nothing is featured yet)
- /portal shows all 54 entries with valid categories

Also: I asked you not to remove the original hardcoded content arrays. Tell me whether 
they were deleted, and if so, restore them as an unused backup file so I have a copy 
of the original data.